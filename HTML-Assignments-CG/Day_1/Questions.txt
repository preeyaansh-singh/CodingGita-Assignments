1. Create an image tag that displays an image named flower.jpg which is in the same folder as your HTML file. Add proper alt text.


2. Display an image named logo.png that is stored inside a folder called images. Your HTML file is outside the images folder.
Also give the image a width of 200px.


3. Your HTML file is inside a folder called pages. The image photo.jpg is in the root folder. Write the correct path to display the image.


4. Display an image with:

src = images/nature.jpg
alt = "Beautiful nature view"
width = 300
height = 200

5. Add a title attribute to an image so that when the user hovers over it, the text “Click to view full size” appears.


6. Create an image that should load only when the user scrolls near it (lazy loading). Use an image from the images folder.


7. Create a hero image that should load immediately when the page opens (eager loading).


8. Write an image tag using loading="auto" and explain in one line what the browser will decide.

9. Create a responsive image using srcset with three versions:

small.jpg (400w)
medium.jpg (800w)
large.jpg (1200w)
Also add a normal src as fallback.


10. Create a complete image tag that includes all these attributes:

Image is inside images/ folder
alt text
width and height
loading="lazy"
title
srcset with two sizes

11. Create two images side by side:

First image loads immediately (eager) and is a logo from assets/logo.png
Second image loads lazily and is a product photo from images/products/shoe.jpg Both should have proper alt text and width of 250px.

Thts it 

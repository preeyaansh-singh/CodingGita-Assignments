### 1.  
Write a complete HTML paragraph that explains a Python function using the correct element for inline code.  
a) The sentence must contain the function name `len()`.  
b) Use the proper tag so that `len()` appears in monospace font.  
c) Make the whole paragraph valid HTML.

### 2.  
Create a multi-line Python function using the correct preformatted element.  
a) Write a factorial function that preserves indentation and line breaks.  
b) Do **not** wrap it inside any other code-related tag.  
c) The code must look exactly as it would in a text editor.

### 3.  
Write an HTML sentence that shows a keyboard shortcut.  
a) Use the correct element for user input.  
b) Display the shortcut `Ctrl + C`.  
c) Surround it with normal paragraph text explaining what the shortcut does.

### 4.  
Write an HTML paragraph that displays sample program output.  
a) Use the correct element for sample output.  
b) Show the text `Hello, World!` as program output.  
c) Keep the rest of the sentence in normal text.

### 5.  
Write an HTML sentence that contains a mathematical or programming variable.  
a) Use the correct element for variables.  
b) Show the formula `F = ma` and highlight the variable `m`.  
c) Make sure the variable is properly marked.

### 6.  
Create a styled multi-line code block.  
a) Write a simple Python `for` loop inside the correct preformatted element.  
b) Add CSS so the block has a dark background (`#1e1e1e`), white text, padding, and rounded corners.  
c) Use a monospace font.

### 7.  
Write HTML that demonstrates the difference between inline code and preformatted code.  
a) One sentence must contain an inline CSS property using the correct inline element.  
b) Below it, show a multi-line Git command sequence using the correct preformatted element.  
c) Do not mix the two elements incorrectly.

### 8.  
Create a paragraph that shows both a keyboard command and its sample output.  
a) Use the correct element for the user-typed command `pwd`.  
b) Use the correct element for the sample output `/home/user/project`.  
c) Keep everything inside one coherent paragraph.

### 9.  
Write a complete HTML snippet that styles the keyboard input element.  
a) Create a sentence that tells the user to press `Enter`.  
b) Style the keyboard element with a light background, border, padding, and rounded corners.  
c) Use a monospace font for the keyboard element.

### 10.  
Write HTML that correctly represents a Bash condition as inline code.  
a) Use the proper inline code element.  
b) The code snippet must be `test -f filename`.  
c) Place it inside a normal explanatory sentence.

### 11.  
Create a preformatted block that displays simple ASCII art.  
a) Use the correct element that preserves whitespace.  
b) Recreate a basic box shape using characters.  
c) Do not add any extra tags inside the preformatted block.

### 12.  
Write a paragraph that contains a programming variable and an assignment.  
a) Use the correct variable element for `count`.  
b) Also mark the full assignment `count = 10` appropriately.  
c) Keep the surrounding text as normal paragraph content.

### 13.  
Write a complete HTML page section that demonstrates three different elements:  
a) One inline code example (any short command or property).  
b) One keyboard shortcut example.  
c) One sample output example.  
Each must use the semantically correct tag and appear in its own sentence.

### 14.  
Create a styled keyboard input and a styled preformatted code block on the same page.  
a) Style `<kbd>` with a bordered, light-background look.  
b) Style `<pre>` with a dark background and white text.  
c) Include at least one example of each element with real content.

### 15.  
Write a complete, valid HTML5 example that follows best practices:  
a) Use `<code>` for a short inline CSS property.  
b) Use `<pre>` for a multi-line code block (any language).  
c) Use `<kbd>` for a keyboard shortcut.  
d) Use `<samp>` for a sample error or output message.  
e) Use `<var>` for a variable name.  
f) Add minimal CSS to improve readability of the `<pre>` and `<kbd>` elements.  
g) Ensure all content is properly escaped if it contains `<` or `>` characters.
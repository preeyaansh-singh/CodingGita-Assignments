### Question 1  
Write three separate paragraphs that correctly display the reserved characters `<`, `>`, and `&`.  

**Guidance:**  
- Use the named entities `&lt;`, `&gt;`, and `&amp;`.  
- Show each character inside a realistic sentence or short code example.  
- Also provide the hexadecimal version of each character in a second set of paragraphs.

### Question 2  
Create a price list that displays three different currency symbols.  

**Guidance:**  
- Show the Euro symbol using both the named entity and the hexadecimal entity.  
- Show the Pound symbol using the named entity and the decimal entity.  
- Show the Yen symbol using any valid entity format.  
- Place each price in its own paragraph.

### Question 3  
Write three mathematical statements that use the correct symbols.  

**Guidance:**  
- Display the “not equal” symbol (`≠`) using the named entity.  
- Display the summation symbol (`∑`) using the hexadecimal entity.  
- Display the infinity symbol (`∞`) using the decimal entity.  
- Put each statement in a separate paragraph.

### Question 4  
Create sentences that correctly use en dash and em dash.  

**Guidance:**  
- Write a year range (e.g., 2023–2025) using the en dash entity.  
- Write a sentence that uses an em dash for emphasis or interruption.  
- Show both the named entity and the hexadecimal entity for each dash.

### Question 5  
Write a copyright and branding footer using the proper legal symbols.  

**Guidance:**  
- Display the copyright symbol (`©`) with the current year using the named entity.  
- Display a registered trademark (`®`) next to a brand name.  
- Display a trademark (`™`) next to a product name.  
- Use at least one hexadecimal or decimal form as well.

### Question 6  
Create a simple navigation-style text that uses directional arrows.  

**Guidance:**  
- Show a left arrow (`←`) using the named entity.  
- Show a right arrow (`→`) using the hexadecimal entity.  
- Show an up arrow (`↑`) using the decimal entity.  
- Place each arrow inside a short phrase (e.g., “Back ←”, “Next →”, “Top ↑”).

### Question 7  
Write three paragraphs that display emojis using numeric entities.  

**Guidance:**  
- Display the smiling face emoji using the hexadecimal entity.  
- Display the check-mark button emoji using the decimal entity.  
- Display the heart symbol using either the named entity or a numeric entity.  
- Add short descriptive text next to each emoji.

### Question 8  
Create a comparison table (using only paragraphs or simple lists) that shows the same symbol in three formats.  

**Guidance:**  
- Choose any one symbol from the notes (for example © or →).  
- Show it once with the named entity (if available).  
- Show it once with the hexadecimal entity.  
- Show it once with the decimal entity.  
- Label each line clearly so the three formats are obvious.

### Question 9  
Write a short product description that mixes several categories of entities.  

**Guidance:**  
- Include at least one reserved character (`<`, `>`, or `&`).  
- Include one currency symbol.  
- Include one mathematical symbol.  
- Include one copyright or trademark symbol.  
- Include one arrow or emoji.  
- Use a mixture of named, hexadecimal, and decimal entities.

### Question 10  
Create a complete mini “Symbol Reference” page section.  

**Guidance:**  
- Start with a heading.  
- Create five short paragraphs, each demonstrating a different category:  
  1. Reserved characters  
  2. Currency symbols  
  3. Mathematical symbols  
  4. Arrows  
  5. Emojis or legal symbols  
- In every paragraph use the correct entity (named preferred when available, otherwise hexadecimal or decimal).  
- Make sure every special character is written with a proper HTML entity and not typed directly.